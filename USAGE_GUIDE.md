# 使用指南

## 三种对话模式详解

### 🔵 普通模式

**适用场景**：日常对话、一般性问题解答

- 使用 `deepseek-chat` 模型
- 快速响应
- 适合大多数日常使用场景

**示例对话**：
- "帮我写一个 Python 排序函数"
- "解释一下什么是闭包"
- "推荐一些前端学习资源"

### 🟣 深度思考模式

**适用场景**：复杂推理、数学问题、逻辑分析

- 使用 `deepseek-reasoner` 模型
- 展示详细的推理过程
- 适合需要深入分析的问题

**特殊功能**：
- 点击"思考过程"按钮可查看 AI 的推理步骤
- 推理内容会实时流式更新
- 帮助理解 AI 如何得出结论

**示例对话**：
- "证明勾股定理"
- "分析这段代码的时间复杂度"
- "解决这道数学应用题：..."

### 🟢 联网搜索模式

**适用场景**：需要最新信息、时事查询

- 使用 `deepseek-chat` 模型 + 联网提示
- 可获取较新的信息
- 适合时效性要求高的问题

**示例对话**：
- "2024年最新的前端框架趋势"
- "最近的科技新闻"
- "当前流行的编程语言排名"

## 移动端使用技巧

### H5 适配说明

项目使用 `postcss-pxtorem` 实现移动端适配：

1. **设计稿基准**：375px 宽度
2. **转换规则**：1px = (1/37.5)rem
3. **支持范围**：320px - 750px 屏幕宽度
4. **自动计算**：页面加载时自动设置根元素字体大小

### 最佳浏览体验

- **推荐屏幕宽度**：375px - 414px (iPhone 标准尺寸)
- **支持横竖屏**：自动适配
- **触摸优化**：禁用了点击高亮，优化了触摸体验

### 移动端调试

使用 Chrome DevTools 的设备模式：

1. 打开 Chrome 开发者工具 (F12)
2. 点击设备切换工具栏 (Ctrl+Shift+M)
3. 选择移动设备（如 iPhone 12 Pro）
4. 刷新页面查看效果

## 界面操作说明

### 模式切换

1. 页面顶部有三个按钮：普通模式、深度思考、联网搜索
2. 点击按钮即可切换模式
3. 当前选中的模式会高亮显示
4. 切换模式不会清空对话历史

### 查看思考过程（深度思考模式专属）

1. 在深度思考模式下发送消息
2. AI 回复时，会出现"思考过程"按钮
3. 点击按钮展开/折叠思考内容
4. 思考过程会实时流式更新

### 输入消息

1. 在底部输入框输入问题
2. 点击"发送"按钮或按回车键发送
3. 等待 AI 回复（会显示加载动画）
4. 回复内容会逐字显示（流式输出）

## 技术细节

### Px 转 Rem 配置

```javascript
// postcss.config.mjs
'postcss-pxtorem': {
  rootValue: 37.5,        // 基准值
  unitPrecision: 5,       // 精度
  propList: ['*'],        // 转换所有属性
  minPixelValue: 2,       // 最小转换值
}
```

### Rem 计算脚本

```javascript
// 动态设置根元素字体大小
function setRem() {
  var docEl = document.documentElement;
  var width = docEl.clientWidth;
  if (width > 750) width = 750;
  if (width < 320) width = 320;
  docEl.style.fontSize = (width / 10) + 'px';
}
```

### API 请求格式

```typescript
// 发送消息时的请求体
{
  message: "用户的问题",
  mode: "normal" | "deepthink" | "websearch"
}
```

### SSE 响应格式

```
// 普通内容
data: {"content": "回答内容"}

// 思考过程（仅深度思考模式）
data: {"type": "thinking", "content": "推理过程"}

// 结束标记
data: [DONE]
```

## 常见问题

### Q: 如何切换深色模式？

A: 应用会自动跟随系统的深色模式设置。在 macOS 上通过系统偏好设置切换，在 Windows 上通过设置 > 个性化 > 颜色切换。

### Q: 深度思考模式的思考过程可以复制吗？

A: 可以。思考过程展开后，可以直接选中文本进行复制。

### Q: 为什么联网搜索模式的信息可能不是最新的？

A: 联网搜索模式依赖于 AI 模型的训练数据和能力，可能无法获取实时信息。建议将其作为参考，重要信息需要额外验证。

### Q: 移动端滚动不流畅怎么办？

A: 尝试以下方法：
1. 关闭浏览器的"流畅滚动"设置
2. 清除浏览器缓存
3. 使用 Safari 或 Chrome 浏览器

### Q: 如何在自己的项目中使用类似的 px 转 rem 方案？

A: 参考以下步骤：
1. 安装 `postcss-pxtorem`
2. 配置 `postcss.config.mjs`
3. 在 HTML 中添加 rem 计算脚本
4. 在 Tailwind 配置中使用 px 单位

## 开发建议

### 自定义样式时的注意事项

1. **使用 px 单位**：在 Tailwind 配置中定义 px 单位，会自动转换为 rem
2. **避免硬编码 rem**：让 postcss-pxtorem 自动处理
3. **特殊情况**：如果某些元素不需要转换，可在 postcss 配置的 `selectorBlackList` 中排除

### 添加新的交互模式

参考现有三种模式的实现：

1. 在前端添加新的模式按钮
2. 在 API 路由中添加对应的处理逻辑
3. 根据需要选择不同的模型或提示词
4. 更新 TypeScript 类型定义

### 性能优化建议

1. 消息过多时考虑实现虚拟滚动
2. 对长文本回复进行分段渲染
3. 缓存用户的模式选择
4. 实现消息持久化（localStorage）

## 更新日志

### v2.0 (当前版本)

- ✨ 新增三种对话模式
- ✨ 新增深度思考过程展示
- ✨ 新增 H5 移动端完美适配
- 🎨 优化了界面设计
- 🐛 修复了若干已知问题

### v1.0

- 🎉 初始版本发布
- ✨ 基础聊天功能
- ✨ SSE 流式输出
- ✨ 深色模式支持
